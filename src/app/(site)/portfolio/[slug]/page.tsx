import type { Metadata } from "next";import { notFound } from "next/navigation";import { sql } from "drizzle-orm";import { Breadcrumb } from "@/components/navigation/Breadcrumb";import { CaseStudy,ProjectOverview,ProjectTools } from "@/components/portfolio/detail/CaseStudy";import { ProjectCTA } from "@/components/portfolio/detail/ProjectCTA";import { ProjectHero } from "@/components/portfolio/detail/ProjectHero";import { ProjectGallery,ProjectMedia } from "@/components/portfolio/detail/ProjectMedia";import { ProjectNavigation } from "@/components/portfolio/detail/ProjectNavigation";import { RelatedProjects } from "@/components/portfolio/detail/RelatedProjects";import { JsonLd } from "@/components/seo/JsonLd";import { PageMotionBoundary } from "@/components/ui/PageMotionBoundary";import { db } from "@/lib/db";import { getAdjacentProjects,getProjectBySlug,getRelatedProjects,getSiteSettings } from "@/lib/db/queries";import { getPageContent } from "@/lib/db/page-content-service";import { absoluteSiteUrl } from "@/lib/seo";import { projectCreativeWorkJsonLd,projectMediaVideoJsonLd } from "@/lib/structured-data";
type SocialSeo={ogTitle:string|null;ogDescription:string|null;ogImageUrl:string|null;twitterTitle:string|null;twitterDescription:string|null;twitterImageUrl:string|null};async function socialSeo(slug:string){const r=await db.execute<SocialSeo>(sql`SELECT og_title AS "ogTitle",og_description AS "ogDescription",og_image_url AS "ogImageUrl",twitter_title AS "twitterTitle",twitter_description AS "twitterDescription",twitter_image_url AS "twitterImageUrl" FROM portfolio_projects WHERE slug=${slug}`);return r.rows[0]}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const[data,settings,social]=await Promise.all([getProjectBySlug(slug),getSiteSettings(),socialSeo(slug)]);if(!data||data.project.status!=="published")return{robots:{index:false,follow:false}};const p=data.project,title=p.seoTitle||p.title,description=p.seoDescription||p.description||`Watch ${p.title}, a video editing project by Lucky Saroj.`,ogTitle=social?.ogTitle||title,ogDescription=social?.ogDescription||description,ogImage=social?.ogImageUrl||p.thumbnailUrl||p.posterUrl||settings?.ogImageUrl||undefined,twitterTitle=social?.twitterTitle||ogTitle,twitterDescription=social?.twitterDescription||ogDescription,twitterImage=social?.twitterImageUrl||ogImage,keywords=[p.title,data.category?.name,p.clientName,p.year?String(p.year):null,"video editing","Lucky Saroj"].filter((v):v is string=>Boolean(v));return{title:title.includes("Lucky Saroj")?{absolute:title}:title,description,keywords,alternates:{canonical:`/portfolio/${p.slug}`},robots:{index:true,follow:true},openGraph:{type:"website",siteName:"Lucky Saroj",title:ogTitle,description:ogDescription,url:`/portfolio/${p.slug}`,...(ogImage?{images:[absoluteSiteUrl(ogImage)]}:{})},twitter:{card:twitterImage?"summary_large_image":"summary",title:twitterTitle,description:twitterDescription,...(twitterImage?{images:[absoluteSiteUrl(twitterImage)]}:{})}}}
export default async function ProjectDetailPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const[data,portfolioPage]=await Promise.all([getProjectBySlug(slug),getPageContent("portfolio")]);if(!data||data.project.status!=="published")notFound();const{project,category,tools,media}=data;const rootVideo=project.videoUrl?.trim();const schemaMedia=media.filter(item=>item.type!=="video"||!rootVideo||item.url.trim()!==rootVideo);const[{prev,next},relatedProjects]=await Promise.all([getAdjacentProjects(slug),getRelatedProjects(project.id,project.categoryId,3)]),copy=portfolioPage.content,detailCopy={overviewEyebrow:copy.detailOverviewEyebrow,clientLabel:copy.detailClientLabel,categoryLabel:copy.detailCategoryLabel,yearLabel:copy.detailYearLabel,caseStudyEyebrow:copy.detailCaseStudyEyebrow,challengeLabel:copy.detailChallengeLabel,approachLabel:copy.detailApproachLabel,resultLabel:copy.detailResultLabel,toolsEyebrow:copy.detailToolsEyebrow,toolsHeading:copy.detailToolsHeading,toolsAriaLabel:copy.detailToolsAriaLabel},mediaCopy={selectedProjectLabel:copy.detailSelectedProjectLabel,mediaEyebrow:copy.detailMediaEyebrow,mediaHeading:copy.detailMediaHeading,previewAltSuffix:copy.detailPreviewAltSuffix,mediaAltSuffix:copy.detailMediaAltSuffix};return <PageMotionBoundary className="bg-[var(--background-primary)] text-[var(--text-primary)]"><JsonLd data={projectCreativeWorkJsonLd({title:project.title,slug:project.slug,description:project.seoDescription??project.description,thumbnailUrl:project.thumbnailUrl??project.posterUrl,videoUrl:project.videoUrl,createdAt:project.createdAt,updatedAt:project.updatedAt,clientName:project.clientName})}/><JsonLd data={projectMediaVideoJsonLd({projectTitle:project.title,projectSlug:project.slug,thumbnailUrl:project.thumbnailUrl??project.posterUrl,media:schemaMedia})}/><Breadcrumb items={[{label:"Home",href:"/"},{label:"Portfolio",href:"/portfolio"},{label:project.title}]}/><ProjectHero project={project} categoryName={category?.name}/><ProjectMedia project={project} categoryName={category?.name} media={media} copy={mediaCopy}/><ProjectOverview project={project} categoryName={category?.name} copy={detailCopy}/><CaseStudy project={project} copy={detailCopy}/><ProjectGallery project={project} media={media} copy={mediaCopy}/><ProjectTools tools={tools} copy={detailCopy}/><RelatedProjects projects={relatedProjects}/><ProjectNavigation previous={prev} next={next} ariaLabel={copy.detailNavigationAriaLabel} previousLabel={copy.detailPreviousLabel} nextLabel={copy.detailNextLabel}/><ProjectCTA eyebrow={copy.detailCtaEyebrow} heading={copy.detailCtaHeading} description={copy.detailCtaDescription} primaryLabel={copy.detailCtaPrimaryLabel} primaryUrl={copy.detailCtaPrimaryUrl} secondaryLabel={copy.detailCtaSecondaryLabel} secondaryUrl={copy.detailCtaSecondaryUrl}/></PageMotionBoundary>}

