"use client";
import { useActionState } from "react";
import { Button } from "@/components/ui/Button";
import { Input, Label, Textarea } from "@/components/ui/Input";
import { FormCard } from "@/components/admin/FormParts";
import { updatePageSeoAction } from "@/lib/actions/page-seo";
import type { PageSeoRecord } from "@/lib/db/page-seo-service";
import { SEO_PAGE_DEFAULTS } from "@/lib/page-seo";
import type { ActionState } from "@/lib/actions/portfolio";
import styles from "./AdminEditorial.module.css";
const initialState:ActionState={status:"idle"};
export function PageSeoForm({seo}:{seo:PageSeoRecord}){const[state,action,pending]=useActionState(updatePageSeoAction,initialState);const page=SEO_PAGE_DEFAULTS[seo.pageKey];return <form action={action} className={styles.sectionStack}><input type="hidden" name="pageKey" value={seo.pageKey}/><input type="hidden" name="revision" value={seo.revision}/><FormCard title={`${page.label} SEO Settings`}><p className={styles.helper}>Main content is the default. Leave override fields blank to reuse Meta → OG → Twitter automatically.</p><div className="grid grid-cols-1 gap-5 lg:grid-cols-2"><div className="lg:col-span-2"><Label htmlFor={`${seo.pageKey}-metaTitle`}>Meta Title</Label><Input id={`${seo.pageKey}-metaTitle`} name="metaTitle" maxLength={200} defaultValue={seo.metaTitle}/></div><div className="lg:col-span-2"><Label htmlFor={`${seo.pageKey}-metaDescription`}>Meta Description</Label><Textarea id={`${seo.pageKey}-metaDescription`} name="metaDescription" rows={3} maxLength={320} defaultValue={seo.metaDescription}/></div><div><Label htmlFor={`${seo.pageKey}-keywords`}>Meta Keywords (optional)</Label><Input id={`${seo.pageKey}-keywords`} name="keywords" maxLength={500} defaultValue={seo.keywords}/></div><div><Label htmlFor={`${seo.pageKey}-canonicalPath`}>Canonical URL / Path</Label><Input id={`${seo.pageKey}-canonicalPath`} name="canonicalPath" maxLength={500} defaultValue={seo.canonicalPath}/></div><div><Label htmlFor={`${seo.pageKey}-pageH1`}>Page H1 override (optional)</Label><Input id={`${seo.pageKey}-pageH1`} name="pageH1" maxLength={200} defaultValue={seo.pageH1} placeholder="Blank = visible primary heading"/></div><div><Label htmlFor={`${seo.pageKey}-pageH2`}>Page H2 override (optional)</Label><Input id={`${seo.pageKey}-pageH2`} name="pageH2" maxLength={200} defaultValue={seo.pageH2} placeholder="Blank = visible section heading"/></div><div><Label htmlFor={`${seo.pageKey}-ogTitle`}>OG Title override</Label><Input id={`${seo.pageKey}-ogTitle`} name="ogTitle" maxLength={200} defaultValue={seo.ogTitle} placeholder="Blank = Meta Title"/></div><div><Label htmlFor={`${seo.pageKey}-ogImageUrl`}>OG Image override</Label><Input id={`${seo.pageKey}-ogImageUrl`} name="ogImageUrl" maxLength={2048} defaultValue={seo.ogImageUrl} placeholder="Blank = global default OG image"/></div><div className="lg:col-span-2"><Label htmlFor={`${seo.pageKey}-ogDescription`}>OG Description override</Label><Textarea id={`${seo.pageKey}-ogDescription`} name="ogDescription" rows={2} maxLength={320} defaultValue={seo.ogDescription} placeholder="Blank = Meta Description"/></div><div><Label htmlFor={`${seo.pageKey}-twitterTitle`}>Twitter Title override</Label><Input id={`${seo.pageKey}-twitterTitle`} name="twitterTitle" maxLength={200} defaultValue={seo.twitterTitle} placeholder="Blank = OG/Meta Title"/></div><div><Label htmlFor={`${seo.pageKey}-twitterImageUrl`}>Twitter Image override</Label><Input id={`${seo.pageKey}-twitterImageUrl`} name="twitterImageUrl" maxLength={2048} defaultValue={seo.twitterImageUrl} placeholder="Blank = OG/global image"/></div><div className="lg:col-span-2"><Label htmlFor={`${seo.pageKey}-twitterDescription`}>Twitter Description override</Label><Textarea id={`${seo.pageKey}-twitterDescription`} name="twitterDescription" rows={2} maxLength={320} defaultValue={seo.twitterDescription} placeholder="Blank = OG/Meta Description"/></div><label className="lg:col-span-2 flex items-center gap-3 rounded-lg border border-white/10 bg-white/[0.025] px-4 py-3 text-sm text-[var(--text-primary)]"><input name="robotsIndex" type="checkbox" defaultChecked={seo.robotsIndex} className="h-4 w-4 accent-[var(--accent-primary)]"/>Allow search engines to index this page</label></div></FormCard>{state.status==="error"&&state.message&&<p className={styles.feedbackError}>{state.message}</p>}{state.status==="success"&&state.message&&<p className={styles.feedbackSuccess}>{state.message}</p>}<div className={styles.saveBar} style={{position:"static",zIndex:"auto",marginTop:0}}><Button type="submit" disabled={pending}>{pending?"Saving...":`Save ${page.label} SEO`}</Button></div></form>}
